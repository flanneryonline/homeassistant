logger:
  default: error
  filters:
    pychromecast.socket_client:
      - "Failed to connect to service ServiceInfo"
      - "Error communicating with socket, resetting connection"
    homeassistant.components.sensor:
      - "Platform smartthings does not generate unique IDs. *"